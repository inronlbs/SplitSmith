package com.splitsmith.app.migration

import android.content.Context

object FolderMigration {
    suspend fun runFolderMigration(context: Context) {
        // No-op migration stub after removing Google Drive integration
    }
}
